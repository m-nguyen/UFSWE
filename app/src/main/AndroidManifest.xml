<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.my.ufswe" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".AboutSWE"
            android:label="@string/app_nav1" >
        </activity>
        <activity
            android:name=".Events"
            android:label="@string/app_nav2" >
        </activity>
        <activity
            android:name=".Officers"
            android:label="@string/app_nav3" >
        </activity>

        <activity
            android:name=".GBMLoginActivity"
            android:label="@string/title_activity_gbmlogin" >
        </activity>
    </application>

</manifest>
